Overview
JS
CSS
jQuery
HTML 5
	latest version of HTML
	multimedia without depending on third party plugins
	new set of UI tags; built-in validations
	Data storage (5 MB per application per browser)
	Drag and Drop API
	GeoLocation
	History API
	WebSockets
	Web Realtime communication
	Canvas API
	File Upload
	
Data Storage -> LocalStorage, SessionStorage, IndexedDB, WebSQL
Stored in the browser memory (typically client machine)
Does not travel automatically back and forth btn the client and the server
Will be available as long as you don't clear cache explicitly. Don't have an expiry date		
	
	
	





$("CSS selector expression").someFunction();

$("expre").val()
$("expre").val("skjdfhasjhfd")

$("expr").text("sdklafhasfdh")
$("expr").text()

$("expr").html("HTML CONTENTS")
$("expr").html()

$('expr').css("property","value")
$('expr').css("property")